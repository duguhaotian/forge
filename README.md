# forge
Forge is an agent-oriented compute broker and workspace runtime.  It provides a unified CLI for discovering, leasing, and operating compute resources across local machines, VMs, and remote hosts.  Built for AI coding agents, Forge treats compute as a programmable resource — not just infrastructure.
